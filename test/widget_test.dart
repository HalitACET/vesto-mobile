// Widget tests for Vesto AI.
//
// Full integration tests with Firebase require mocking (firebase_auth_mocks /
// fake_cloud_firestore). These will be added in a future sprint.
// For now this file is intentionally minimal to keep CI green.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — remove once real widget tests are added', () {
    expect(true, isTrue);
  });
}
